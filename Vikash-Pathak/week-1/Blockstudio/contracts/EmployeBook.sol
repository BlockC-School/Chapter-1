// SPDX-Licence-idendifier: MIT

pragma solidity ^0.8.0;

pragma experimental ABIEncoderV2;



contract EmpData {
    
   struct Employee {
        uint id;
        string name;
        string email;
        uint256 wallet;
        uint age;
        uint salary;
    }
        Employee[] employees;
  
constructor employees(string memory _name, string memory _email, uint _age) internal returns(  // constructor must have to be payable or not-payable
        Employee.name , _name;
        Employee.email = _email;
        Employee.age = _age;
    )
    
    // Defining a Structure
    struct Users {
        string name;
        string subject;
        uint8 marks;
    }
 
    // Creating a structure object
    student public std1;
 
    // Defining a function to return
    // values of the elements of the structure
    function structure() public view returns(
     
    // Creating a mapping
    mapping (address => student) result;
    address[] student_result;
}
